[package]
name = "argrep"
version = "0.1.0"
edition = "2024"
authors = ["Artem Shemet"]
description = "Fast parallel text search utility (Rust version)"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
crossbeam-channel = "0.5"
colored = "3.1"
glob = "0.3"
